.table-responsive
  %table.table.table-striped
    %thead
      %tr
        %th= t :'country.name'
        %th= t :nb_of_residents
    %tbody
      - @countries.each do |country|
        %tr
          %td= country.name
          %td= country.residents.count
