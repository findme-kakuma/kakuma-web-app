#your-profile
  = render partial: 'header'

  = simple_form_for @resident, url: wizard_path do |form|
    = render partial: 'profile_form', locals: { form: form, scope_i18n: :'workflow.your_profile.profile_form' }
    = form.button :submit, 'Next', class: 'pull-right btn-lg btn-success'
