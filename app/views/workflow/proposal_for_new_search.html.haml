#new-search
  %div
    - if session.key? :force_new_search
      .row.header
        - if session[:force_new_search] == 'false'
          %h3 Person added !
          %p.text-center
            You will receive a text message notification if this person registers on
            Find Me.
      %h3 Add another person ?
    - else
      .row.header
        %h3 You are registered on Find Me !
        %p.text-center
          You can now describe a person you are looking for in order to be
          notified if this person registers on Find Me.
      %h3 Add a person you are looking for ?
    = link_to 'yes', wizard_path(:your_search, force_new_search: true),
              class: 'btn btn-default btn-lg'
    = link_to 'no', next_wizard_path, class: 'btn btn-default btn-lg'
